import Vue from 'vue';//'vue';
import Router from 'vue-router';
Vue.use(Router);

export default class FluentManagement {

    constructor() {
        this.Vue = Vue;
        this.Router = Router;
    }
    
}
